import React from 'react';

// const client = new MapboxClient(YOUR_API_ACCESS_TOKEN);

// client.getTile('mapbox.streets', { x: 0, y: 0, z: 0 }, function(err, data) {
//     // data is a map tile in PNG format
//   });
export default function map() {
  return <div>map</div>;
}
